---
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>
  <body>
    <div class="wrap">
      <nav class="nav">
        <a class="brand" href={import.meta.env.BASE_URL}></a>
        <div>
          <a href={import.meta.env.BASE_URL}>Projects</a>
        </div>
      </nav>
      <slot />
    </div>
  </body>
</html>
