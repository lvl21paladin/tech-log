import { defineConfig } from 'astro/config';

// Project page on GitHub Pages: https://YOUR-USERNAME.github.io/dev-log
// Change 'site' to your username. If the repo is named something other than "dev-log",
// change 'base' accordingly. If the repo is named YOUR-USERNAME.github.io
// (user page, not project page), set base to '/'.
export default defineConfig({
  site: 'https://lvl21paladin.github.io',
  base: '/tech-log/',
});
